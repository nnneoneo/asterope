version: "3.9"

networks:
  web:
    external: true

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asterope-frontend
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.neovara.rule=Host(`asterope.neovara.ltd`)"
      - "traefik.http.routers.neovara.entrypoints=websecure"
      - "traefik.http.routers.neovara.tls.certresolver=le"
      - "traefik.http.services.neovara.loadbalancer.server.port=80"
    networks:
      - web
